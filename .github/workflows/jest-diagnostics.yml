name: Jest diagnostics

on:
  pull_request:
    branches: ["main"]

permissions:
  contents: write

jobs:
  diagnose:
    if: github.actor != 'github-actions[bot]' && github.event.pull_request.head.ref == 'agent/use-npm-jest'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Capture diagnostics
        run: |
          npm run check > jest-check.log 2>&1 || true
          npm test > jest-test.log 2>&1 || true
      - name: Commit diagnostics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add jest-check.log jest-test.log
          git commit -m "Capture Jest diagnostics"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
